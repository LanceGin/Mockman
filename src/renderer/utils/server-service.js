// server method with dynamic configuration

export default class serverService {
  constructor(props) {
    super(props);
  
    this.state = {};
  }

  // start server
  start() {}

  // stop server
  stop() {}

  // set cors
  serCors() {}

  // ser routes
  serRoutes() {}
}
